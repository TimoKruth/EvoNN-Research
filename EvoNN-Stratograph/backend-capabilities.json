{
  "schema_version": "1.0.0",
  "system": "stratograph",
  "runtime_role": "engine_skeleton",
  "capabilities": [
    {
      "id": "mlx_native",
      "platforms": ["darwin-arm64"],
      "implemented": false,
      "dependency": "mlx",
      "dependency_condition": "Declared as mlx>=0.25,<1 with the exact marker sys_platform == 'darwin' and platform_machine == 'arm64'; bootstrap probe only, not qualified."
    },
    {
      "id": "numpy_fallback",
      "platforms": ["darwin", "linux"],
      "implemented": false,
      "dependency": "numpy",
      "dependency_condition": "Declared as numpy>=2.1,<3 on all engine platforms; bootstrap probe only, not qualified."
    }
  ],
  "evidence": {
    "scientific": false,
    "portability": false,
    "producer_conformance": false
  }
}
