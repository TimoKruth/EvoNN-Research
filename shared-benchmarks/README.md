# Shared Benchmarks

Data-only directory; Python schemas and loaders live in `evonn_shared`.
See the central [catalog/cache guide](../README.md#benchmark-and-lm-cache-storage)
and [current capabilities](../README.md#current-capabilities).
Run `scripts/ci/benchmarks-checks.sh` from the repository root.
